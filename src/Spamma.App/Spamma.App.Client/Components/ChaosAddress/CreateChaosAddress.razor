@using Spamma.Modules.DomainManagement.Client.Application.Commands
@inject ICommander Commander

@code {
    [Parameter]
    public Guid SubdomainId { get; set; }

    [Parameter]
    public EventCallback OnCreated { get; set; }

    public void Open()
    {
        // Hook into standard modal flow (slidout/confirm) per project patterns.
    }

    private async Task Create()
    {
        // Placeholder: call Commander.Send(new CreateChaosAddressCommand(...))
        await Task.Delay(10);
        if (OnCreated.HasDelegate) await OnCreated.InvokeAsync(null);
    }
}

<div class="hidden" id="chaos-create-modal">
    <!-- Modal placeholder. Use project's standard modal/slidout patterns when implementing UI behaviours. -->
    <div class="p-4">
        <h3 class="text-lg font-medium">Create Chaos Address</h3>
        <div class="mt-3 text-sm text-gray-500">Form (local-part, SMTP code dropdown, optional description) will appear here.</div>
        <div class="mt-4">
            <button class="px-3 py-1 bg-blue-600 text-white rounded" @onclick="Create">Create</button>
        </div>
    </div>
</div>

 
